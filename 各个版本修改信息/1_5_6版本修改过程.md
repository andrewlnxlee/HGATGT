● 实现完成，结构没问题。我更新下任务。

● 已改完，核心是把 H-GAT-GT 的点级 ID 生成改成和 baseline 同类的两阶段链路。

  这次改动：
  - sim_env/evaluate_single.py:22
    - 引入 GNNPostProcessor
  - sim_env/evaluate_single.py:292
    - 新增 build_group_detections()，从点构造群检测中心和 shape
  - sim_env/evaluate_single.py:314
    - 新增 run_hgat_point_identity_pipeline()
    - 流程是：
        - 点级输入
      - DBSCAN 群检测
      - GNNPostProcessor 做群级时序跟踪
      - project_cluster_tracks_to_points() 把群 track ID 投影回点
      - GroupConstrainedPointAssociator 生成最终点级 ID
  - sim_env/evaluate_single.py:368
    - H-GAT-GT 主分支不再用 GNNPointPostProcessor
    - 改成 gnn_group_tracker + gnn_point_assoc
  - sim_env/evaluate_single.py:408
    - Meas Ablation 同步切到同一条 ID 生成链路
  - sim_env/evaluate_single.py:420
    - No Uncertainty Gating 也同步切到同一条链路
  - sim_env/evaluate_single.py:490
    - 输出里明确提示：H-GAT-GT 点级 ID 现在使用 group tracking + group-constrained point association

  我刻意没动：
  - trackers/gnn_processor_single.py
  - metrics.py
  - sim_env/config.py

  也就是说：
  - 保留你当前最有效的 clutter filtering 修复
  - 保留 POINT_ID_SWITCH_GAP_TOLERANCE = 0
  - 只修正 H-GAT-GT 在点级评估里的 ID 生成结构不公平问题

  已做语法检查，通过。

  你现在在服务器只需要跑：
  - python sim_env/evaluate_single.py

  重点看这几个：
  - H-GAT-GT (Ours) 的 IDSW
  - MOTA
  - OSPA(Loc) / RMSE(Pos) 是否稳住
  - OSPA(Card) / Count Err / FAR 不要反弹

  把新输出发我，我来判断这次是不是已经真正打到 MOTA 主因。